/**
 * Island ids that have a built React app in `@platform/motionsites-islands`.
 * Keep in sync with `packages/motionsites-islands/ready.json`.
 */
export const MOTIONSITES_ISLAND_READY = ['velorah-hero', 'asme-hero'] as const
